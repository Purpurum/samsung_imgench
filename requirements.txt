# ============================================================
# Python-зависимости проекта
# ============================================================

# Базовые
pyyaml>=6.0
numpy>=1.24,<2.0
pillow>=10.0
opencv-python-headless  # без GUI, только алгоритмы для resize/метриков
# Spark (версия должна совпадать с образом apache/spark:3.5.0)
pyspark==3.5.0

# Работа с HDFS: pyarrow даёт native-доступ через libhdfs
pyarrow>=14.0.0

# Тестирование
pytest>=7.4
pytest-cov>=4.1

# ------------------------------------------------------------
# Опциональные зависимости для продакшн-режима (use_mock: false)
# Раскомментируйте, если нужна реальная модель satlaspretrain:
# ------------------------------------------------------------
# torch>=2.0           # PyTorch (CPU-версия; для GPU см. pytorch.org)
# torchvision>=0.15
# satlaspretrain_models>=0.3.0
# rasterio>=1.3        # для работы с GeoTIFF с сохранением геопривязки
